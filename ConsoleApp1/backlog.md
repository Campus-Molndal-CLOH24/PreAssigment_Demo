# Backlog - Metod för Metod

## Skapa klasser

- Skapa klassdiagram
- Skapa en klass för objektet (t.ex. Book).
- Skapa en klass för att hantera samlingen (t.ex. Library).
- Implementera attribut i objektklassen

- Lägg till attribut som title, author, och year i Book-klassen.
- Skapa getter-metoder för att hämta värdena för dessa attribut.
- Skapa en datastruktur i samlingsklassen

- Implementera en List<Book> eller Book[] i Library-klassen för att hantera en samling böcker.
- Lägg till metod för att lägga till objekt

- Skapa en metod addBook(Book book) i Library som lägger till böcker till samlingen.
- Implementera metod för att lista objekt

- Skapa en metod listBooks() i Library som skriver ut alla böcker i samlingen.
- Skapa metod för att ta bort objekt

- Implementera en metod removeBook(String title) som tar bort en bok baserat på dess titel.
- Implementera sökfunktion

- Skapa en metod searchBook(String title) som söker efter en bok baserat på dess titel och returnerar boken eller ett meddelande om att boken inte finns.
- Lägg till sorteringsfunktion

- Implementera en metod sortBooksByTitle() som sorterar böckerna i listan efter titel.
- Testkör alla metoder

- Testkör varje metod individuellt för att säkerställa att de fungerar korrekt.
- Testkör hela flödet genom att lägga till, lista, söka efter, ta bort och sortera böcker.

- Uppdatera klassdiagram

- Uppdatera Readme som berättar om er kamp mot koden!